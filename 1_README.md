# Multivariate-statistical-analysis

Лабораторная работа “ Арифметические операции в Python”

Основные арифметические операции: 
+      *   /    // (целочисленное деление)    % (остаток от деления)   ** (степень, м. б.  и дробная или отрицательная)
Типы данных:
целые (34, -2345….)  вещественные (-0.456, 1234.05…) строковые (Привет, мир', Hello ,’z’…) логические (True, False)  комплексные  (complex (3,4) или 3+4j…)
Задание: нужно проверить для каждого типа данных, какие операции могут выполняться и как. Проверку для каждого типа нужно выполнить в своей ячейке, которую 
снабдить в первой строке комментарием (начинается с #). Предварительно откроем в Google Colab блокнот Арифметические операции.ipynb, созданный в предыдущей 
работе (файл/открыть блокнот  или ctrl/o).

1.	В первой кодовой ячейке набираем 
# арифметические операции над целыми числами.
заводим две переменные (например, a и b) и присваиваем им произвольные целые значения (a=67…)  
переменная c — для результата (c=a+b);  результаты распечатываем с пояснением
 print(a, “+” , b , ”=”, c)
 запускаем ячейку на выполнение.
Дальше c=a-b , снова print и ячейку выполняем.
 …………..
c=a**b	
с=a**(-0.5)   # корень квадратный в знаменателе

2.	Повторяем пункт 1 для каждого типа данных, добавляя в блокнот по новой кодовой ячейке.
При работе со строками попробуйте переменную a умножить на целую положительную константу —например, с=a*5, потом можно     с=a*2+4*b+a)

3.	Попробуем поработать с тригонометрическими функциями (аргумент — в радианах)
import math   # импортируем модуль math
x=3.14159
y=math.sin(x)
print(x,y)
Список функций можно распечатать:
print(dir(math))   # каталог math (например, math.fabs(x) —модуль x)





